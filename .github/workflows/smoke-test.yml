name: Smoke Test
on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/smoke.sh
        env:
          API_URL: ${{ secrets.API_URL }}
